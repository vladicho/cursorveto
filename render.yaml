services:
  - type: web
    name: moldelab
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: MOLDELAB_JWT_SECRET
        generateValue: true
